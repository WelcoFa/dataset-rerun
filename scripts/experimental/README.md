Experimental and legacy scripts live here so the top-level `scripts/` folder stays focused on the main supported entrypoints.

- `gigahands/`: older or exploratory GigaHands viewers and experiments
- `misc/`: one-off helpers or legacy wrappers that are not part of the main documented workflow
