# App UI

This folder contains a minimal static dashboard shell for dataset selection, viewer control, and status logging.

Open `index.html` directly in a browser, or serve the folder from any static file server.
